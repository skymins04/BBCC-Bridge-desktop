interface localStorage {
  position: {
    x: number;
    y: number;
  };
  size: {
    width: number;
    height: number;
  };
  ignoreMouseEvent: {
    state: boolean;
  };
  showBorder: {
    state: boolean;
  };
}
